# Rules applied when THIS module is minified. It is not — see the note in
# `AdManageKit/proguard-rules.pro`. Rules that ship to consumers live in
# `consumer-rules.pro`.
#
# The Yandex Mobile Ads SDK ships its own `-keepnames class yads.**` and
# related rules from its AAR; do not restate them here.

-keepattributes SourceFile,LineNumberTable
